{
    "name": "log-analyzer-system",
    "version": "1.0.0",
    "description": "Robust Log Analyzer CLI tool and browser React dashboard client.",
    "scripts": {
        "dev": "npm --prefix dashboard run dev",
        "build": "npm --prefix dashboard run build",
        "preview": "npm --prefix dashboard run preview",
        "generate-logs": "node scripts/generate_logs.js",
        "analyze": "node logAnalyzer.js"
    }
}